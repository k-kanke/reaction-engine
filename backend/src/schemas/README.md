# schemas

Shared TypeBox schemas.

Responsibilities:

- Define `realtime_feature`, `feedback_event`, session, transcript, and media ref schemas.
- Export TypeScript types from schema definitions.
- Keep Chrome extension payloads and backend validation aligned.
