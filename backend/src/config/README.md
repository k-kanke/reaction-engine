# config

Environment configuration.

Responsibilities:

- Load and validate environment variables.
- Keep Cloud Run service config explicit.
- Centralize project id, Pub/Sub topic names, bucket names, Redis settings, and database settings.
