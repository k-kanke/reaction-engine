# observability

Logging and monitoring helpers.

Responsibilities:

- Provide structured logging conventions.
- Track latency for WebSocket ingest, Redis writes, Pub/Sub publish, writer persistence, and feedback decisions.
- Emit metrics suitable for Cloud Logging / Cloud Monitoring.
