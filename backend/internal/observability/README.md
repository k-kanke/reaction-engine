# observability

Logging, metrics, and tracing helpers.

Responsibilities:

- Structured logs with `session_id`, `audience_id`, `capture_id`, and `event_id`.
- Latency and error metrics for Redis, Pub/Sub, Cloud SQL, Storage, Speech-to-Text, and LLM calls.
