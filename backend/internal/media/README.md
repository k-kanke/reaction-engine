# media

Media API internals.

Responsibilities:

- Signed upload URL issuance.
- `media_ref` and `capture_snapshot` persistence.
- Upload completion verification.
- `media_uploaded` event publishing.
